services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: kale
      POSTGRES_PASSWORD: kale_dev
      POSTGRES_DB: kale
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
